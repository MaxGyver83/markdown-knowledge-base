services:
  backend:
    build: ../backend/markdown-api
    ports:
      - "8085:8080"
    volumes:
      - data:/app/data

  frontend:
    build: ../frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  data:
